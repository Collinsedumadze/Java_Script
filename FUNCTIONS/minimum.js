// Like the standard function Math.min that returns its smallest argument. Define the function min that takes two arguments and returns their minimum.

function getMin(x , y){
    let min = 0;
    if(x > y){
       return min += y; 
    }else{
       return min += x;
    }

}

console.log(getMin(0, 4));